

import Foundation
import Combine

protocol OCFManagerProtocol {
//    func apiRequest(speaker: Speaker, apiInfo: APIInfoProtocol) -> AnyPublisher<String, Never>?
}

final class OCFManager: OCFManagerProtocol {
//    func apiRequest(speaker: Speaker, apiInfo: APIInfoProtocol) -> AnyPublisher<String, Never>? {
//        
//    }
    
//    static func convertToAttributes(params: [String: Any]) -> String {
//        let encoder = JSONEncoder()
//        encoder.outputFormatting = .prettyPrinted // Định dạng JSON gọn gàng hơn

//        do {
//          let jsonData = try encoder.encode(params)
//          let jsonString = String(data: jsonData, encoding: .utf8)!
//          print(jsonString)
//        } catch {
//          print("Error encoding JSON:", error)
//        }
//    }
}
